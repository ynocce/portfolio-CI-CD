#Pipeline de Deploy no Azure Portal
#Serviço usado: Azure Static Web App
#Pipeline no repos do github

trigger:
- main

pool:
  vmImage: ubuntu-latest

stages:
  - stage: CopyFiles
    jobs:
      - job: CopyFiles
        steps:
          - task: CopyFiles@2
            displayName: "Copiando Arquivos do repos"
            inputs:
              Contents: 'portifolio.i2131313ntegrado.azureDevOps/**'
              TargetFolder: $(build.artdad23wifactstagingdirectory)

          - task: PublishBuildArtifacts@1
            displayName: "Criando um artefato com os arquivos"
            inputs:
              PathtoPublish: '$(Build.ArtifactStagingDirectory)'
              ArtifactName: 'drop'